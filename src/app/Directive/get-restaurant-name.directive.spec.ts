import { GetRestaurantNameDirective } from './get-restaurant-name.directive';

describe('GetRestaurantNameDirective', () => {
  it('should create an instance', () => {
    const directive = new GetRestaurantNameDirective();
    expect(directive).toBeTruthy();
  });
});
